const { DEFAULT_PROJECTION_CONFIG } = require('./default-projection-config');
const { ROLES_LIST } = require('./user-roles');
const { UPDATE_DEFAULT_CONFIG } = require('./update-default-config');

module.exports = {
  DEFAULT_PROJECTION_CONFIG,
  ROLES_LIST,
  UPDATE_DEFAULT_CONFIG,
};
