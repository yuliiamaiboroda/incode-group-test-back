const ChangeBossOfUserSchema = require('./change-boss-of-user');
const loginSchema = require('./login.schema');
const registerSchema = require('./register.schema');

module.exports = { registerSchema, loginSchema, ChangeBossOfUserSchema };
